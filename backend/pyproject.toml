[project]
name = "backend"
version = "0.1.0"
description = "ContentForge AI - FastAPI Core Backend, AI Engine & Renderers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
    "pytest>=8.0.0",
]
